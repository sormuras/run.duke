project:
  name: Duke
  versionPattern: JAVA-MODULE
  snapshot:
    pattern: .*-ea.*

release:
  github:
    tagName: '{{projectVersion}}'
    releaseName: 'Duke {{projectEffectiveVersion}}'

files:
  globs:
    - pattern: .duke/modules/*.jar
